# Habilitar paginação por default para o endpoint /produtos
- No maximo 10 itens por página
- Se o cliente HTTP nao passar nenhum parametro, o default é a pagina 1, com 10 itens por pagina

# Habilitar busca por descrição (opcional)
- permitir um filtro de produtos por descricão